---
import { getEntry } from "astro:content";
import MarkdownPage from "../components/MarkdownPage.astro";

const entry = await getEntry("pages", "gemeindeleben");
---

<MarkdownPage entry={entry} />
